configuration:
  AZURE_BICEP_FILE_EXPANSION: true

input:
  pathIgnore:
    - 'modules'

# rule:
#   includeLocal: true

# Configure binding options
binding:
  targetType:
  - 'resourceType'
  - 'type'