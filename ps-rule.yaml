configuration:
  AZURE_BICEP_FILE_EXPANSION: true

input:
  pathIgnore:
    - 'modules/**/*.bicep'
    - '!modules/**/*.tests.bicep'

# rule:
#   includeLocal: true

# Configure binding options
binding:
  targetType:
  - 'resourceType'
  - 'type'