configuration:
  AZURE_BICEP_FILE_EXPANSION: true

input:
  pathIgnore:
    - 'modules/**/*.bicep'
    - '!modules/**/*.tests.bicep'

rule:
  includeLocal: true
  exclude:
    - "Azure.Resource.UseTags"

# Configure binding options
binding:
  targetType:
  - 'resourceType'
  - 'type'